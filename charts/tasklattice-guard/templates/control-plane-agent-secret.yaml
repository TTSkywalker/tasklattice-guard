{{- include "tasklattice-guard.validateValues" . }}
{{- if and (not .Values.controlPlaneAgent.deepseek.existingSecret) .Values.controlPlaneAgent.deepseek.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tasklattice-guard.deepseekSecretName" . }}
  labels:
    {{- include "tasklattice-guard.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.controlPlaneAgent.deepseek.secretKey }}: {{ .Values.controlPlaneAgent.deepseek.apiKey | quote }}
{{- end }}
