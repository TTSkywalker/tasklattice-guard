{{- include "tali-guard.validateValues" . }}
{{- if and (not .Values.playgroundChat.existingSecret) .Values.playgroundChat.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tali-guard.playgroundChatSecretName" . }}
  labels:
    {{- include "tali-guard.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.playgroundChat.secretKey }}: {{ .Values.playgroundChat.apiKey | quote }}
{{- end }}
